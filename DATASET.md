# FaceOLAT Capture Gallery

The mosaic below shows all 407 released capture IDs and expressions. Each capture ID is treated as a separate dataset subject. Matching label colors connect expressions captured from the same participant.

![FaceOLAT capture IDs and expressions](assets/faceolat-capture-mosaic.jpg)
